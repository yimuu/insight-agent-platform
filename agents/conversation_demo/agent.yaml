api_version: insight.agent/v1
kind: agent

metadata:
  id: conversation_demo
  name: Conversation Demo
  description: Deterministic local passthrough fixture for terminal-only Conversation qualification.

inputs:
  message: any
  conversation_context: any

output: any

workflow:
  steps:
    - return: $message
