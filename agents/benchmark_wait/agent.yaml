api_version: insight.agent/v1
kind: agent

metadata:
  id: benchmark_wait
  name: Benchmark Durable Wait
  description: Holds one durable signal wait for active-Run capacity qualification.

inputs: {}
output: string

execution:
  persistence_mode: full

workflow:
  steps:
    - id: gate
      wait:
        signal: continue
        response: string
    - return: $gate
