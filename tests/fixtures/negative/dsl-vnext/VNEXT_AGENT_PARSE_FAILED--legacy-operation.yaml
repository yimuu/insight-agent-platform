api_version: insight.agent/v2
kind: agent
metadata: {id: legacy_operation, name: Legacy Operation, description: Negative fixture.}
inputs: {}
output: string
workflow:
  steps:
    - type: operation
      id: answer
      call: current_time
      inputs: {}
  result:
    return: unreachable
