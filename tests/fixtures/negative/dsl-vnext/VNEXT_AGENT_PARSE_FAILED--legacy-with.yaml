api_version: insight.agent/v2
kind: agent
metadata: {id: legacy_with, name: Legacy With, description: Negative fixture.}
inputs: {}
output: string
workflow:
  steps:
    - type: action
      id: now
      call: current_time
      with:
        timezone: UTC
  result:
    return: unreachable
