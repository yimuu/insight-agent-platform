api_version: insight.agent/v2
kind: agent
metadata: {id: legacy_parts, name: Legacy Parts, description: Negative fixture.}
inputs: {}
output: string
workflow:
  steps:
    - type: llm
      id: answer
      model: general_chat
      messages:
        - role: user
          parts:
            - text: This old message field must fail.
      response: string
  result:
    return: $answer
