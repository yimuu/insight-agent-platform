api_version: insight.agent/v2
kind: agent
metadata: {id: legacy_parts, name: Legacy Parts, description: Negative fixture.}
schema_dialect: https://json-schema.org/draft/2020-12/schema
input:
  schema: {type: object}
output:
  data_schema: {type: "null"}
workflow:
  steps:
    - kind: llm
      id: answer
      model: general_chat
      messages:
        - role: user
          parts:
            - {kind: text, text: This old content shape must fail.}
      response: {format: text}
  result:
    return:
      data: {literal: null}
