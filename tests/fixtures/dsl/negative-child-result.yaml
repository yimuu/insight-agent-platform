api_version: insight.agent/v1
kind: agent
inputs: {}
output: string
workflow:
  steps:
    - id: group
      parallel:
        one:
          - id: answer
            type: action
            call: fixture.answer
            response: string
      result:
        return: $answer
