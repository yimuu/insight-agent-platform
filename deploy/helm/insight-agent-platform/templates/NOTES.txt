Insight Agent Platform is installed as release {{ .Release.Name }}.

Check readiness:
  kubectl -n {{ .Release.Namespace }} get pods
  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "insight-agent-platform.fullname" . }} 3000:{{ .Values.service.port }}
  curl http://127.0.0.1:3000/health/ready

{{- if .Values.postgresql.enabled }}
The embedded PostgreSQL instance is intended for local evaluation and benchmarks.
Use postgresql.enabled=false with an external managed PostgreSQL service for production.
{{- end }}
Artifact and File content use the configured S3/RustFS bucket. Preserve that
bucket together with the durable PostgreSQL database.
