image:
  repository: insight-agent-platform
  digest: sha256:dddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddd
  pullPolicy: IfNotPresent
imagePullSecrets: []
candidate:
  deploymentConfigDigest: sha256:cccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccc
namespace: platform-mcp-host
replicas: 2
service: {port: 9443}
observability: {port: 9090}
config:
  existingConfigMap: insight-platform-mcp-host-candidate
  fileKey: mcp-host.json
  digest: sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
serverTls:
  existingSecret: insight-platform-mcp-host-server-tls
  keys: {clientCa: client-ca.pem, certificate: server.pem, privateKey: server-key.pem}
egressTls:
  existingSecret: insight-platform-mcp-host-egress-client-tls
  keys: {ca: ca.pem, certificate: client.pem, privateKey: client-key.pem}
networkPolicy:
  enabled: true
  callerNamespace: platform-capability-remote
  callerPodSelector: {app.kubernetes.io/component: capability-remote-worker}
  egressNamespace: platform-egress
  egressPodSelector: {app.kubernetes.io/component: egress-broker}
  egressPort: 8443
  dnsNamespaceSelector: {kubernetes.io/metadata.name: kube-system}
  dnsPodSelector: {k8s-app: kube-dns}
  monitoringNamespaceSelector: {insight.platform/monitoring-namespace: "true"}
  monitoringPodSelector: {app.kubernetes.io/name: prometheus}
monitoring: {enabled: true, interval: 30s, scrapeTimeout: 5s}
resources:
  requests: {cpu: 250m, memory: 256Mi, ephemeral-storage: 64Mi}
  limits: {cpu: "2", memory: 1Gi, ephemeral-storage: 1Gi}
podDisruptionBudget: {enabled: true, minAvailable: 1}
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 16
  targetCPUUtilizationPercentage: 70
resourcePool:
  enabled: true
  replicas: 2
  service: {port: 9543}
  observability: {port: 9190}
  config:
    existingConfigMap: insight-platform-mcp-resource-host-candidate
    fileKey: mcp-resource-host.json
    digest: sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
  database:
    existingSecret: insight-platform-mcp-resource-host-database
    urlKey: database-url
  serverTls:
    existingSecret: insight-platform-mcp-resource-host-server-tls
    keys: {clientCa: client-ca.pem, certificate: server.pem, privateKey: server-key.pem}
  resources:
    requests: {cpu: 250m, memory: 256Mi, ephemeral-storage: 64Mi}
    limits: {cpu: "2", memory: 1Gi, ephemeral-storage: 1Gi}
  podDisruptionBudget: {enabled: true, minAvailable: 1}
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 16
    targetCPUUtilizationPercentage: 70
  callerNamespace: platform-context-worker
  callerPodSelector: {app.kubernetes.io/component: context-subscription-worker}
  postgresCidrs: [10.10.0.0/24]
discoveryPool:
  enabled: true
  replicas: 2
  observability: {port: 9290}
  config:
    existingConfigMap: insight-platform-mcp-discovery-worker-candidate
    fileKey: mcp-discovery-worker.json
    digest: sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
  database:
    existingSecret: insight-platform-mcp-discovery-worker-database
    urlKey: database-url
  clientTls:
    existingSecret: insight-platform-mcp-discovery-worker-client-tls
    keys: {certificate: client.pem, privateKey: client-key.pem}
  egressTls:
    existingSecret: insight-platform-mcp-discovery-worker-egress-ca
    caKey: ca.pem
  artifactTls:
    existingSecret: insight-platform-mcp-discovery-worker-artifact-ca
    caKey: ca.pem
  resources:
    requests: {cpu: 250m, memory: 256Mi, ephemeral-storage: 64Mi}
    limits: {cpu: "2", memory: 1Gi, ephemeral-storage: 1Gi}
  podDisruptionBudget: {enabled: true, minAvailable: 1}
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 16
    targetCPUUtilizationPercentage: 70
  postgresCidrs: [10.10.0.0/24]
  artifactNamespace: platform-artifacts
  artifactPodSelector: {app.kubernetes.io/component: artifact-data-worker}
  artifactPort: 8443
subscriptionPool:
  enabled: true
  replicas: 2
  observability: {port: 9390}
  config:
    existingConfigMap: insight-platform-mcp-subscription-worker-candidate
    fileKey: mcp-subscription-worker.json
    digest: sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
  database:
    existingSecret: insight-platform-mcp-subscription-worker-database
    urlKey: database-url
  clientTls:
    existingSecret: insight-platform-mcp-subscription-worker-client-tls
    keys: {certificate: client.pem, privateKey: client-key.pem}
  egressTls:
    existingSecret: insight-platform-mcp-subscription-worker-egress-ca
    caKey: ca.pem
  resources:
    requests: {cpu: 250m, memory: 256Mi, ephemeral-storage: 64Mi}
    limits: {cpu: "2", memory: 1Gi, ephemeral-storage: 1Gi}
  podDisruptionBudget: {enabled: true, minAvailable: 1}
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 16
    targetCPUUtilizationPercentage: 70
  postgresCidrs: [10.10.0.0/24]
