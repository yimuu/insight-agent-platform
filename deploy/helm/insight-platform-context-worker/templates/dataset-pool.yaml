{{- if .Values.datasetPool.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata: {name: {{ include "insight-platform-context-worker.datasetName" . }}, namespace: {{ .Values.namespace }}}
automountServiceAccountToken: false
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-platform-context-worker.datasetName" . }}
  namespace: {{ .Values.namespace }}
  labels: {{ include "insight-platform-context-worker.datasetLabels" . | nindent 4 }}
spec:
  selector: {{ include "insight-platform-context-worker.datasetLabels" . | nindent 4 }}
  ports: [{name: observability, port: {{ int .Values.datasetPool.observability.port }}, targetPort: observability, protocol: TCP}]
---
apiVersion: apps/v1
kind: Deployment
metadata: {name: {{ include "insight-platform-context-worker.datasetName" . }}, namespace: {{ .Values.namespace }}}
spec:
  replicas: {{ int .Values.datasetPool.replicas }}
  strategy: {type: RollingUpdate, rollingUpdate: {maxUnavailable: 0, maxSurge: 1}}
  selector:
    matchLabels:
      {{- include "insight-platform-context-worker.datasetLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "insight-platform-context-worker.datasetLabels" . | nindent 8 }}
        insight.platform/workload-role: context-dataset-worker
        insight.platform/component-role: context_dataset_worker
      annotations:
        insight.platform/deployment-config-digest: {{ .Values.candidate.deploymentConfigDigest | quote }}
    spec:
      serviceAccountName: {{ include "insight-platform-context-worker.datasetName" . }}
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 150
      imagePullSecrets: {{ toYaml .Values.imagePullSecrets | nindent 8 }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: context-dataset-worker
          image: {{ include "insight-platform-context-worker.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/usr/local/bin/platform-context-dataset-worker"]
          ports: [{name: observability, containerPort: {{ int .Values.datasetPool.observability.port }}, protocol: TCP}]
          env:
            - {name: PLATFORM_CONTEXT_DATASET_WORKER_CONFIG, value: /etc/insight/config/context-dataset-worker.json}
            - {name: PLATFORM_CONTEXT_DATASET_WORKER_CONFIG_DIGEST, value: {{ .Values.datasetPool.config.digest | quote }} }
            - name: PLATFORM_CONTEXT_DATASET_WORKER_DATABASE_URL
              valueFrom: {secretKeyRef: {name: {{ .Values.datasetPool.database.existingSecret }}, key: {{ .Values.datasetPool.database.urlKey }}}}
            - {name: PLATFORM_CONTEXT_DATASET_WORKER_ARTIFACT_CA_PATH, value: /etc/insight/artifact-tls/ca.pem}
            - {name: PLATFORM_CONTEXT_DATASET_WORKER_CLIENT_CERT_PATH, value: /etc/insight/artifact-tls/client.pem}
            - {name: PLATFORM_CONTEXT_DATASET_WORKER_CLIENT_KEY_PATH, value: /etc/insight/artifact-tls/client-key.pem}
          startupProbe: {httpGet: {path: /livez, port: observability}, periodSeconds: 2, timeoutSeconds: 2, failureThreshold: 30}
          readinessProbe: {httpGet: {path: /readyz, port: observability}, periodSeconds: 5, timeoutSeconds: 2, failureThreshold: 3}
          livenessProbe: {httpGet: {path: /livez, port: observability}, periodSeconds: 10, timeoutSeconds: 2, failureThreshold: 6}
          resources: {{ toYaml .Values.datasetPool.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: ["ALL"]}
          volumeMounts:
            - {name: config, mountPath: /etc/insight/config, readOnly: true}
            - {name: artifact-tls, mountPath: /etc/insight/artifact-tls, readOnly: true}
            - {name: tmp, mountPath: /tmp}
      volumes:
        - name: config
          configMap:
            name: {{ .Values.datasetPool.config.existingConfigMap }}
            items: [{key: {{ .Values.datasetPool.config.fileKey }}, path: context-dataset-worker.json}]
        - name: artifact-tls
          secret:
            secretName: {{ .Values.datasetPool.artifactTls.existingSecret }}
            items:
              - {key: {{ .Values.datasetPool.artifactTls.keys.ca }}, path: ca.pem}
              - {key: {{ .Values.datasetPool.artifactTls.keys.certificate }}, path: client.pem}
              - {key: {{ .Values.datasetPool.artifactTls.keys.privateKey }}, path: client-key.pem}
        - {name: tmp, emptyDir: {sizeLimit: 32Mi}}
{{- if .Values.datasetPool.podDisruptionBudget.enabled }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: {name: {{ include "insight-platform-context-worker.datasetName" . }}, namespace: {{ .Values.namespace }}}
spec:
  minAvailable: {{ int .Values.datasetPool.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "insight-platform-context-worker.datasetLabels" . | nindent 6 }}
{{- end }}
{{- if .Values.datasetPool.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: {{ include "insight-platform-context-worker.datasetName" . }}, namespace: {{ .Values.namespace }}}
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: {{ include "insight-platform-context-worker.datasetName" . }}}
  minReplicas: {{ int .Values.datasetPool.autoscaling.minReplicas }}
  maxReplicas: {{ int .Values.datasetPool.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource: {name: cpu, target: {type: Utilization, averageUtilization: {{ int .Values.datasetPool.autoscaling.targetCPUUtilizationPercentage }}}}
{{- end }}
{{- if .Values.monitoring.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: {name: {{ include "insight-platform-context-worker.datasetName" . }}, namespace: {{ .Values.namespace }}}
spec:
  namespaceSelector: {matchNames: [{{ .Values.namespace }}]}
  selector:
    matchLabels: {{ include "insight-platform-context-worker.datasetLabels" . | nindent 6 }}
  endpoints: [{port: observability, path: /metrics, interval: {{ .Values.monitoring.interval }}, scrapeTimeout: {{ .Values.monitoring.scrapeTimeout }}, scheme: http}]
{{- end }}
{{- end }}
