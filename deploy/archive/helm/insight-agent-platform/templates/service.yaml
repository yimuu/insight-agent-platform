apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-agent-platform.fullname" . }}
  labels:
    {{- include "insight-agent-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "insight-agent-platform.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
